#pragma once

// #include "lightgraph.h"

#include <vector>

template <typename T>
class WikiSearch
{
public:
    WikiSearch();

private:
    // LightGraph<unsigned int> graph_;
    std::vector<int> test;
};
